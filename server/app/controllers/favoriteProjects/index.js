const { getFavoriteProjects } = require('./getFavoriteProjects')
const { favoriteProjects } = require('./favoriteProjects')
const { unfavoriteProjects } = require('./unfavoriteProjects')

module.exports = {
  getFavoriteProjects,
  favoriteProjects,
  unfavoriteProjects
}
